export const revalidate = 0
export const dynamic = 'force-dynamic'

import { SimilarityClient } from '@/components/tools/SimilarityClient'

export default function SimilarityPage() {
  return <SimilarityClient />
}
